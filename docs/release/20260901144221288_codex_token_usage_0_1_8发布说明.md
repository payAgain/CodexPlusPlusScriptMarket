# Codex Token Usage 0.1.8 发布说明

生成时间：2026-09-01 14:42

## 版本

- 插件：`Codex Token Usage`
- 版本：`0.1.8`

## 本次修复

### 1. 徽标误嵌入 Shell 执行结果块

部分对话中，token 统计徽标会被插入到 Shell / 工具执行卡片内部而不是助手消息末尾：

- 移除 `closestAssistantContainer` 的提前退出（原逻辑在内层工具卡片分数达到 10 时停止向上查找，导致携带"喜欢/不喜欢"按钮的消息根容器从未参与比较）
- `scoreAssistantContainer` 对以 `Shell` / `终端` / `已运行` / `正在运行` 等为首个标签的容器直接返回负分
- 兜底选择器路径过滤 `data-message-author-role="tool"` 等工具节点

### 2. 市场仓库迁移

`index.json` 中本仓库脚本的 `script_url` 与 `homepage` 统一切换到 `payAgain/CodexPlusPlusScriptMarket`，此后由 fork 仓库自行维护。
